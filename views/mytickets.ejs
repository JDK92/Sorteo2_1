<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" type="text/css" href="/css/materialize.min.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <div class="container center-align">
        <% if(objValidator == true) { %>
            <h1>HOLA MUNDO</h1>
            <% } %>
    </div>

    <div class="container center-align">

        <% if(datos.length > 0){ %>

            <h3>
                Total de boletos:
            </h3>

            <div class="row">
                <div class="col s4 offset-s4 materialize-red white-text z-depth-2">
                    <h2>
                        <%= datos.length %>
                    </h2>
                </div>
            </div>

            <h3>Mis boletos</h3>
            <div class="row grey lighten-3">
                <% for (var i = 0; i < datos.length; i++) { %>
                    <div class="col s3">
                        <h6>
                            <strong>
                                <%= datos[i] %>
                            </strong>
                        </h6>
                    </div>
                    <% } %>
            </div>

            <div class="row">
                <div class="col s12 m6 offset-m3">
                    <a href="uploadticket" class="waves-effect btn">
                        Registrar más facturas
                    </a>
                </div>
            </div>
            <% } else{ %>
                <p class="flow-text">Todavía no tienes boletos. Haz clic en el botón para registrarlos.</p>
                <a href="uploadticket" class="btn">Registrar facturas</a>
                <% } %>

                    <a href=".">Cerrar sesión</a>
    </div>



    <script src="/js/materialize.min.js"></script>
</body>

</html>